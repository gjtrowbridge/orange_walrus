<% provide(:title, "Edit Activity") %>
<h1>Edit Activity</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@activity) do |f| %>
        <%= render partial: 'fields', locals: { :f => f} %>
        <%= f.submit "Update Activity", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>